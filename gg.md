```python import os from textwrap import dedent root = "/mnt/data/nextspice_runtime_full_refactor" runtime_dir = os.path.join(root, "runtime") analyses_dir = os.path.join(runtime_dir, "analyses") math_dir = os.path.join(runtime_dir, "math_solvers") os.makedirs(analyses_dir, exist_ok=True) os.makedirs(math_dir, exist_ok=True) files = {} files[os.path.join(runtime_dir, "__init__.py")] = dedent('''\ from .solver_result import SolverResult from .options import SimulatorOptions from .mna_builder import MNABuilder from .simulator import Simulator ''') files[os.path.join(runtime_dir, "solver_result.py")] = dedent('''\ class SolverResult: def __init__(self, x=None, status="SUCCESS", error_msg="", residual=0.0, solve_time=0.0, method_used=""): self.x = x self.status = status self.error_msg = error_msg self.residual = residual self.solve_time = solve_time self.method_used = method_used def __repr__(self): res_str = "N/A" if self.residual is None else "%.2e" % self.residual method = "" if not self.method_used else ", method=%s" % self.method_used return "SolverResult(status=%s, res=%s, time=%.2fms%s)" % ( self.status, res_str, self.solve_time * 1000.0, method ) ''') files[os.path.join(runtime_dir, "options.py")] = dedent('''\ class SimulatorOptions: """ .OPTIONS 控制參數。 """ def __init__(self, options_dict=None): opts = options_dict or {} self.reltol = float(opts.get('RELTOL', 1e-3)) self.abstol = float(opts.get('ABSTOL', 1e-6)) self.itl1 = int(opts.get('ITL1', 100)) self.itl4 = int(opts.get('ITL4', 100)) self.gmin = float(opts.get('GMIN', 1e-12)) self.solver = str(opts.get('SOLVER', 'spsolve')).lower() self.method = str(opts.get('METHOD', 'TRAP')).upper() self.damping = str(opts.get('DAMPING', 'AUTO')).upper() self.srcsteps = int(opts.get('SRCSTEPS', 0)) self.linear_tol = float(opts.get('LINTOL', 1e-10)) self.linear_maxiter = int(opts.get('LINMAXITER', 1000)) self.precond = bool(opts.get('PRECOND', True)) ''') files[os.path.join(runtime_dir, "mna_builder.py")] = dedent('''\ import scipy.sparse class MNABuilder: """ 專責： 1. extra variable 配置 2. context 建立 3. matrix / rhs 建構 4. element stamping 相容層 """ def __init__(self, circuit): self.circuit = circuit self.node_mgr = circuit.node_mgr self.dim = 0 self.extra_var_map = {} self.extra_by_name = {} def prepare(self): node_count = self.node_mgr.num_unknowns curr_extra_idx = node_count self.extra_var_map = {} for el in self.circuit.elements: if getattr(el, 'extra_vars', 0) > 0: self.extra_var_map[el] = curr_extra_idx curr_extra_idx += el.extra_vars self.dim = curr_extra_idx self.extra_by_name = {} for el, idx in self.extra_var_map.items(): self.extra_by_name[el.name.upper()] = idx return self.dim def make_ctx(self, mode, freq=None, t=None, dt=None): return { 'mode': mode, 'freq': freq, 't': t, 'dt': dt, 'extra_map': self.extra_var_map, 'extra_by_name': self.extra_by_name, } def build_real_system(self, x_guess=None, ctx=None): A = scipy.sparse.lil_matrix((self.dim, self.dim), dtype=float) import numpy as np b = np.zeros(self.dim, dtype=float) self.stamp_system(A, b, ctx or {}, x_guess=x_guess) return A, b def build_complex_system(self, ctx=None): A = scipy.sparse.lil_matrix((self.dim, self.dim), dtype=complex) import numpy as np b = np.zeros(self.dim, dtype=complex) self.stamp_system(A, b, ctx or {}, x_guess=None) return A, b def stamp_system(self, A, b, ctx, x_guess=None): if x_guess is not None: ctx['current_x'] = x_guess for el in self.circuit.elements: extra_idx = self.extra_var_map.get(el) if getattr(el, 'is_nonlinear', False): self._stamp_nonlinear_compatible(el, A, b, x_guess, extra_idx, ctx) else: el.stamp(A, b, extra_idx=extra_idx, ctx=ctx) def _stamp_nonlinear_compatible(self, el, A, b, x_guess, extra_idx, ctx): """ 相容你目前專案裡幾種不同的 stamp_nonlinear 介面。 目標是讓 runtime 先穩定跑起來，再慢慢統一 element ABI。 """ # 第一優先：新介面 try: return el.stamp_nonlinear(A, b, x_guess, extra_idx=extra_idx, ctx=ctx) except TypeError: pass # 第二優先：舊介面（x_old, A, b, ...) try: return el.stamp_nonlinear(x_guess, A, b, extra_idx=extra_idx, ctx=ctx) except TypeError: pass # 第三優先：更舊介面（含 node mapping） try: return el.stamp_nonlinear(A, b, x_guess, self.node_mgr.mapping) except TypeError: pass # 最後再試一次最老順序 return el.stamp_nonlinear(x_guess, A, b, self.node_mgr.mapping) ''') files[os.path.join(math_dir, "__init__.py")] = dedent('''\ from .linear import linear_solve, LinearSolveResult from .newton import newton_solve, NewtonResult ''') files[os.path.join(math_dir, "linear.py")] = dedent('''\ import time import numpy as np import scipy.sparse import scipy.sparse.linalg ITERATIVE_SOLVERS = { 'gmres': scipy.sparse.linalg.gmres, 'bicgstab': scipy.sparse.linalg.bicgstab, 'cgs': scipy.sparse.linalg.cgs, 'lgmres': scipy.sparse.linalg.lgmres, } class LinearSolveResult: def __init__(self, x=None, status="SUCCESS", error_msg="", residual=None, solve_time=0.0, method_used="", matrix_shape=None): self.x = x self.status = status self.error_msg = error_msg self.residual = residual self.solve_time = solve_time self.method_used = method_used self.matrix_shape = matrix_shape def __repr__(self): res_str = "N/A" if self.residual is None else "%.2e" % self.residual return "LinearSolveResult(status=%s, residual=%s, time=%.2fms, method=%s, shape=%s)" % ( self.status, res_str, self.solve_time * 1000.0, self.method_used, self.matrix_shape ) def _to_csr(A): if scipy.sparse.isspmatrix_csr(A): return A if scipy.sparse.issparse(A): return A.tocsr() return scipy.sparse.csr_matrix(A) def _compute_residual(A_csr, x, b): try: r = A_csr.dot(x) - b return float(np.max(np.abs(r))) except Exception: return None def _build_preconditioner(A_csr, drop_tol): try: ilu = scipy.sparse.linalg.spilu(A_csr.tocsc(), drop_tol=drop_tol) return scipy.sparse.linalg.LinearOperator(A_csr.shape, ilu.solve) except Exception: return None def linear_solve(A, b, method='spsolve', tol=1e-10, maxiter=1000, precond=True, ilu_drop_tol=1e-4, fallback_direct=True): t0 = time.time() method = str(method).lower().strip() A_csr = _to_csr(A) if A_csr.shape[0] != A_csr.shape[1]: return LinearSolveResult( status="FAILURE", error_msg="Matrix must be square, got shape=%s" % (A_csr.shape,), solve_time=time.time() - t0, method_used=method, matrix_shape=A_csr.shape ) if method in ('spsolve', 'lu'): try: x = scipy.sparse.linalg.spsolve(A_csr, b) return LinearSolveResult( x=x, residual=_compute_residual(A_csr, x, b), solve_time=time.time() - t0, method_used='spsolve', matrix_shape=A_csr.shape ) except Exception as e: return LinearSolveResult( status="FAILURE", error_msg=str(e), solve_time=time.time() - t0, method_used='spsolve', matrix_shape=A_csr.shape ) solver_fn = ITERATIVE_SOLVERS.get(method) if solver_fn is None: method = 'spsolve' try: x = scipy.sparse.linalg.spsolve(A_csr, b) return LinearSolveResult( x=x, residual=_compute_residual(A_csr, x, b), solve_time=time.time() - t0, method_used='spsolve', matrix_shape=A_csr.shape ) except Exception as e: return LinearSolveResult( status="FAILURE", error_msg=str(e), solve_time=time.time() - t0, method_used='spsolve', matrix_shape=A_csr.shape ) M = None if precond: M = _build_preconditioner(A_csr, ilu_drop_tol) try: x, info = solver_fn(A_csr, b, tol=tol, maxiter=maxiter, M=M) except Exception as e: if not fallback_direct: return LinearSolveResult( status="FAILURE", error_msg=str(e), solve_time=time.time() - t0, method_used=method, matrix_shape=A_csr.shape ) try: x = scipy.sparse.linalg.spsolve(A_csr, b) return LinearSolveResult( x=x, residual=_compute_residual(A_csr, x, b), solve_time=time.time() - t0, method_used=method + "->spsolve", matrix_shape=A_csr.shape ) except Exception as e2: return LinearSolveResult( status="FAILURE", error_msg="Iterative failed: %s ; fallback failed: %s" % (str(e), str(e2)), solve_time=time.time() - t0, method_used=method, matrix_shape=A_csr.shape ) if info != 0: if not fallback_direct: return LinearSolveResult( status="FAILURE", error_msg="Iterative solver did not converge, info=%s" % info, solve_time=time.time() - t0, method_used=method, matrix_shape=A_csr.shape ) try: x = scipy.sparse.linalg.spsolve(A_csr, b) return LinearSolveResult( x=x, residual=_compute_residual(A_csr, x, b), solve_time=time.time() - t0, method_used=method + "->spsolve", matrix_shape=A_csr.shape ) except Exception as e: return LinearSolveResult( status="FAILURE", error_msg="Iterative info=%s ; fallback failed: %s" % (info, str(e)), solve_time=time.time() - t0, method_used=method, matrix_shape=A_csr.shape ) return LinearSolveResult( x=x, residual=_compute_residual(A_csr, x, b), solve_time=time.time() - t0, method_used=method, matrix_shape=A_csr.shape ) ''') files[os.path.join(math_dir, "newton.py")] = dedent('''\ import time import numpy as np from .linear import linear_solve class NewtonResult: def __init__(self, x=None, status="SUCCESS", error_msg="", residual=None, solve_time=0.0, method_used="", iterations=0, converged=False, last_matrix=None): self.x = x self.status = status self.error_msg = error_msg self.residual = residual self.solve_time = solve_time self.method_used = method_used self.iterations = iterations self.converged = converged self.last_matrix = last_matrix def __repr__(self): res_str = "N/A" if self.residual is None else "%.2e" % self.residual return "NewtonResult(status=%s, converged=%s, iters=%d, residual=%s, time=%.2fms, method=%s)" % ( self.status, self.converged, self.iterations, res_str, self.solve_time * 1000.0, self.method_used ) def default_convergence_check(x_new, x_old, reltol, abstol): diff = np.abs(x_new - x_old) tol = reltol * np.maximum(np.abs(x_new), np.abs(x_old)) + abstol return bool(np.all(diff <= tol)) def _apply_damping(x_old, x_new, prev_norm, mode): diff = x_new - x_old diff_norm = np.linalg.norm(diff) if mode == 'OFF': return x_new, diff_norm if mode == 'ON': alpha = 0.5 return x_old + alpha * diff, diff_norm if diff_norm > prev_norm * 2.0: alpha = min(1.0, prev_norm / (diff_norm + 1e-30)) alpha = max(alpha, 0.1) return x_old + alpha * diff, diff_norm return x_new, diff_norm def newton_solve(dim, build_system_fn, max_iters=100, reltol=1e-3, abstol=1e-6, x_init=None, damping='AUTO', linear_method='spsolve', linear_tol=1e-10, linear_maxiter=1000, linear_precond=True, convergence_check_fn=None): t0 = time.time() convergence_check_fn = convergence_check_fn or default_convergence_check if x_init is None: x_guess = np.zeros(dim, dtype=float) else: x_guess = np.array(x_init, dtype=float, copy=True) prev_norm = float('inf') method_used = '' last_matrix = None for it in range(1, max_iters + 1): try: A, b = build_system_fn(x_guess) except Exception as e: return NewtonResult( status="FAILURE", error_msg="build_system_fn failed: %s" % str(e), solve_time=time.time() - t0, iterations=it - 1, converged=False ) last_matrix = A lin_res = linear_solve( A=A, b=b, method=linear_method, tol=linear_tol, maxiter=linear_maxiter, precond=linear_precond ) method_used = lin_res.method_used if lin_res.status != "SUCCESS": return NewtonResult( status="FAILURE", error_msg="linear solve failed: %s" % lin_res.error_msg, solve_time=time.time() - t0, method_used=method_used, iterations=it - 1, converged=False, last_matrix=last_matrix ) x_new = np.array(lin_res.x, dtype=float, copy=False) x_new, diff_norm = _apply_damping(x_guess, x_new, prev_norm, str(damping).upper()) prev_norm = diff_norm if convergence_check_fn(x_new, x_guess, reltol, abstol): return NewtonResult( x=x_new, status="SUCCESS", residual=lin_res.residual, solve_time=time.time() - t0, method_used=method_used, iterations=it, converged=True, last_matrix=last_matrix ) x_guess = x_new return NewtonResult( status="NON_CONVERGENCE", error_msg="NR failed to converge after %d iterations" % max_iters, solve_time=time.time() - t0, method_used=method_used, iterations=max_iters, converged=False, last_matrix=last_matrix ) ''') files[os.path.join(analyses_dir, "__init__.py")] = dedent('''\ from .base import AnalysisBase from .op import OPAnalysis from .ac import ACAnalysis from .tran import TRANAnalysis ''') files[os.path.join(analyses_dir, "base.py")] = dedent('''\ import time import numpy as np import scipy.sparse class AnalysisBase: def __init__(self, simulator): self.sim = simulator self.circuit = simulator.circuit self.node_mgr = simulator.node_mgr self.opts = simulator.opts self.mna = simulator.mna def prepare(self): return self.mna.prepare() def make_ctx(self, mode, freq=None, t=None, dt=None): return self.mna.make_ctx(mode=mode, freq=freq, t=t, dt=dt) def stamp_system(self, A, b, ctx, x_guess=None): return self.mna.stamp_system(A, b, ctx, x_guess=x_guess) def start_timer(self): return time.time() def elapsed(self, t0): return time.time() - t0 def make_solver_result(self, x=None, status="SUCCESS", error_msg="", residual=0.0, solve_time=0.0, method_used=""): return self.sim.result_class( x=x, status=status, error_msg=error_msg, residual=residual, solve_time=solve_time, method_used=method_used ) def empty_result(self, msg="No unknown variables."): return self.make_solver_result(status="EMPTY", error_msg=msg) def build_real_system(self, dim, ctx, x_guess=None): A = scipy.sparse.lil_matrix((dim, dim), dtype=float) b = np.zeros(dim, dtype=float) self.stamp_system(A, b, ctx, x_guess=x_guess) return A, b def build_complex_system(self, dim, ctx): A = scipy.sparse.lil_matrix((dim, dim), dtype=complex) b = np.zeros(dim, dtype=complex) for el in self.circuit.elements: extra_idx = self.mna.extra_var_map.get(el) el.stamp(A, b, extra_idx=extra_idx, ctx=ctx) return A, b ''') files[os.path.join(analyses_dir, "op.py")] = dedent('''\ import numpy as np from .base import AnalysisBase from ..math_solvers.newton import newton_solve class OPAnalysis(AnalysisBase): def run(self, ctx=None, max_iters=None, reltol=None, abstol=None): dim = self.prepare() if dim == 0: return self.empty_result() if ctx is None: ctx = self.make_ctx(mode='op') max_iters = max_iters or self.opts.itl1 reltol = reltol or self.opts.reltol abstol = abstol or self.opts.abstol t0 = self.start_timer() def build_system_fn(x_guess): return self.build_real_system(dim, ctx, x_guess=x_guess) nr_res = newton_solve( dim=dim, build_system_fn=build_system_fn, max_iters=max_iters, reltol=reltol, abstol=abstol, x_init=None, damping=self.opts.damping, linear_method=self.opts.solver, linear_tol=self.opts.linear_tol, linear_maxiter=self.opts.linear_maxiter, linear_precond=self.opts.precond ) if nr_res.status == "SUCCESS": return self.make_solver_result( x=nr_res.x, residual=nr_res.residual, solve_time=self.elapsed(t0), method_used=nr_res.method_used ) stepped = self._run_source_stepping(dim, ctx, max_iters, reltol, abstol) if stepped[0] is not None: return self.make_solver_result( x=stepped[0], residual=stepped[1], solve_time=self.elapsed(t0), method_used=stepped[2] ) return self.make_solver_result( status="NON_CONVERGENCE", error_msg="OP failed after NR + Source Stepping. Last: %s" % nr_res.error_msg, solve_time=self.elapsed(t0) ) def _iter_scalable_sources(self): for el in self.circuit.elements: if hasattr(el, 'dc_value'): yield el, 'dc_value', el.dc_value elif hasattr(el, 'value') and el.name.upper().startswith(('V', 'I')): yield el, 'value', el.value def _run_source_stepping(self, dim, ctx, max_iters, reltol, abstol): src_steps = self.opts.srcsteps if self.opts.srcsteps > 0 else 10 saved = [] for el, attr, val in self._iter_scalable_sources(): saved.append((el, attr, val)) x_guess = np.zeros(dim, dtype=float) last_residual = None last_method = "" try: for step in range(1, src_steps + 1): scale = float(step) / float(src_steps) for el, attr, val in saved: setattr(el, attr, val * scale) def build_system_fn(step_guess): return self.build_real_system(dim, ctx, x_guess=step_guess) nr_res = newton_solve( dim=dim, build_system_fn=build_system_fn, max_iters=max_iters, reltol=reltol, abstol=abstol, x_init=x_guess, damping='ON', linear_method=self.opts.solver, linear_tol=self.opts.linear_tol, linear_maxiter=self.opts.linear_maxiter, linear_precond=self.opts.precond ) if nr_res.status != "SUCCESS": return None, None, None x_guess = nr_res.x last_residual = nr_res.residual last_method = "srcstep(%d/%d)+%s" % (step, src_steps, nr_res.method_used) return x_guess, last_residual, last_method finally: for el, attr, val in saved: setattr(el, attr, val) ''') files[os.path.join(analyses_dir, "ac.py")] = dedent('''\ import math import numpy as np import scipy.sparse.linalg from .base import AnalysisBase class ACAnalysis(AnalysisBase): def build_freqs(self, f_start, f_stop, points, sweep_type='DEC'): sweep_type = str(sweep_type).upper() if f_start <= 0 or f_stop <= 0: raise ValueError("AC sweep frequency must be > 0") if f_stop < f_start: raise ValueError("AC sweep stop frequency must be >= start frequency") if points <= 0: raise ValueError("AC sweep points must be > 0") if sweep_type == 'DEC': decades = math.log10(f_stop / f_start) if f_stop > f_start else 0.0 total_points = int(round(points * decades)) + 1 return np.logspace(np.log10(f_start), np.log10(f_stop), total_points) if sweep_type == 'OCT': octaves = math.log2(f_stop / f_start) if f_stop > f_start else 0.0 total_points = int(round(points * octaves)) + 1 return np.logspace(np.log10(f_start), np.log10(f_stop), total_points) if sweep_type == 'LIN': return np.linspace(f_start, f_stop, points) raise ValueError("Unsupported AC sweep: %s" % sweep_type) def run(self, f_start, f_stop, points, sweep_type='DEC'): dim = self.prepare() if dim == 0: return [] freqs = self.build_freqs(f_start, f_stop, points, sweep_type) results = [] for f in freqs: ctx = self.make_ctx(mode='ac', freq=f) A, b = self.build_complex_system(dim, ctx) try: A_csr = A.tocsr() x = scipy.sparse.linalg.spsolve(A_csr, b) residual = np.max(np.abs(A_csr.dot(x) - b)) results.append({ "freq": float(f), "x": x, "status": "SUCCESS", "residual": float(residual) }) except Exception as e: results.append({ "freq": float(f), "x": None, "status": "FAILURE", "error_msg": str(e) }) return results ''') files[os.path.join(analyses_dir, "tran.py")] = dedent('''\ import numpy as np from .base import AnalysisBase from ..math_solvers.newton import newton_solve from nextspice.engine.integration import get_integration_method class TRANAnalysis(AnalysisBase): def run(self, tstep, tstop, max_iters=None, reltol=None, abstol=None): dim = self.prepare() if dim == 0: return [] if tstep <= 0: return [{"status": "ERROR", "msg": "tstep must be > 0"}] if tstop < 0: return [{"status": "ERROR", "msg": "tstop must be >= 0"}] max_iters = max_iters or self.opts.itl4 reltol = reltol or self.opts.reltol abstol = abstol or self.opts.abstol op_res = self._run_initial_op() if op_res.status != "SUCCESS": return [{"status": "ERROR", "msg": "Initial OP failed: %s" % op_res.error_msg}] x_prev = op_res.x x_prev2 = None results = [{"time": 0.0, "x": x_prev.copy(), "status": "SUCCESS"}] first_integrator = self._select_integrator(first_step=True) self._update_all_histories(x_prev, tstep, first_integrator) t_points = np.arange(tstep, tstop + (tstep * 0.1), tstep) first_step = True for t in t_points: integrator = self._select_integrator(first_step=first_step) def build_system_fn(x_guess): ctx = self.make_ctx(mode='tran', t=t, dt=tstep) ctx['current_x'] = x_guess ctx['integrator'] = integrator ctx['x_prev'] = x_prev ctx['x_prev2'] = x_prev2 return self.build_real_system(dim, ctx, x_guess=x_guess) nr_res = newton_solve( dim=dim, build_system_fn=build_system_fn, max_iters=max_iters, reltol=reltol, abstol=abstol, x_init=x_prev.copy(), damping=self.opts.damping, linear_method=self.opts.solver, linear_tol=self.opts.linear_tol, linear_maxiter=self.opts.linear_maxiter, linear_precond=self.opts.precond ) if nr_res.status != "SUCCESS": results.append({ "time": float(t), "status": "FAILURE", "msg": nr_res.error_msg }) break x_new = nr_res.x results.append({ "time": float(t), "x": x_new.copy(), "status": "SUCCESS" }) self._update_all_histories(x_new, tstep, integrator) x_prev2 = x_prev x_prev = x_new first_step = False return results def _run_initial_op(self): saved_dc = {} for el in self.circuit.elements: if hasattr(el, '_eval_tran_voltage') and getattr(el, 'tran', None): saved_dc[el] = el.dc_value el.dc_value = el._eval_tran_voltage(0.0) elif hasattr(el, '_eval_tran_current') and getattr(el, 'tran', None): saved_dc[el] = el.dc_value el.dc_value = el._eval_tran_current(0.0) try: return self.sim.op.run(ctx=self.make_ctx(mode='op')) finally: for el, val in saved_dc.items(): el.dc_value = val def _select_integrator(self, first_step=False): method = str(self.opts.method).upper() if method == 'GEAR2': if first_step: return get_integration_method('trapezoidal') return get_integration_method('gear2') if method == 'BE': return get_integration_method('trapezoidal') return get_integration_method('trapezoidal') def _update_all_histories(self, x, dt, integrator): ctx = { 'dt': dt, 'integrator': integrator } for el in self.circuit.elements: if hasattr(el, 'update_history'): el.update_history( x, extra_idx=self.mna.extra_var_map.get(el), ctx=ctx ) ''') files[os.path.join(runtime_dir, "simulator.py")] = dedent('''\ from .solver_result import SolverResult from .options import SimulatorOptions from .mna_builder import MNABuilder from .analyses import OPAnalysis, ACAnalysis, TRANAnalysis class Simulator: """ 重構後的總控台。 這一層只負責： 1. 持有 circuit / options 2. 建立 MNA builder 3. 建立 analyses 4. 對外 dispatch """ def __init__(self, circuit, options=None): self.circuit = circuit self.node_mgr = circuit.node_mgr self.opts = options if isinstance(options, SimulatorOptions) else SimulatorOptions(options) self.result_class = SolverResult self.mna = MNABuilder(circuit) self.op = OPAnalysis(self) self.ac = ACAnalysis(self) self.tran = TRANAnalysis(self) def solve_op(self, **kwargs): return self.op.run(**kwargs) def solve_ac(self, *args, **kwargs): return self.ac.run(*args, **kwargs) def solve_tran(self, *args, **kwargs): return self.tran.run(*args, **kwargs) def get_full_report(self, solution_vec): if solution_vec is None: return {} report = self.circuit.get_voltage_report(solution_vec) for el, idx in self.mna.extra_var_map.items(): if idx < len(solution_vec): report["I(%s)" % el.name] = solution_vec[idx] return report ''') files[os.path.join(root, "README.txt")] = dedent('''\ 這一包是把 runtime 主要骨架一次整合好的版本： runtime/ ├── __init__.py ├── solver_result.py ├── options.py ├── mna_builder.py ├── math_solvers/ │ ├── __init__.py │ ├── linear.py │ └── newton.py ├── analyses/ │ ├── __init__.py │ ├── base.py │ ├── op.py │ ├── ac.py │ └── tran.py └── simulator.py 重點： 1. linear / newton 已完全獨立成純數學核心 2. OP / AC / TRAN 已拆成三個 analysis 3. MNA 低階細節抽到 mna_builder.py 4. simulator.py 只負責 dispatch 5. nonlinear stamping 有相容層，能過渡你目前專案裡不同 stamp_nonlinear 介面 你接下來還要補的唯一大事： - passives.py 真的改成吃 ctx['integrator'] - 最終統一 nonlinear 元件 ABI - 如果需要 BE，就再補一個 backward_euler.py 到 engine/integration/ 目前這一包已經可以當正式重構起點。 ''') for path, content in files.items(): with open(path, "w", encoding="utf-8") as f: f.write(content) print(root) for path in sorted(files): print(os.path.relpath(path, root)) ```